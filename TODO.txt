1. Consolidate classes in other two OAuth Agents
   Delete log4j files

2. Make SSL enabled conditional on SERVER_CERT_P12_PATH being set
   No changes to deployment interface then
   Add example configuration properties to README

3. Deployment testing against branches for Node.js and both Kotlin OAuth agents

4. Fix Spring 3 issues with injecting OAuthAgentConfiguration

   Wiremock has problems, meaning standalone should be used.
   But then test configuration is no longer autowired
   https://github.com/wiremock/wiremock/issues/1760

STRETCH
-------
5. .NET OAuth Agent

6. Next.js OAuth Agent